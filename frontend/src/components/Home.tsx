import React from "react";
import Button from "@mui/material/Button";
const Home: React.FC = () => {
	return (
		<Button variant="contained" color="success">
			Success
		</Button>
	);
};

export default Home;
